/*2023-10-03T11:13:00-01:00*/

/********************************************************************
 * analog_pump.c
 * 
 * generated by: plcgen 3.1.391
 ********************************************************************/

#include "cb_comp.h"
#include "iec_1131.h"

#include "analog_pump.h"



/**************************** Variables *****************************/

CB_Index analog_pump__num = 0;
CB_Mem_Bool *analog_pump_voltage380;
CB_Mem_Bool *analog_pump_cmdSupply;
CB_Mem_Bool *analog_pump_sensorSupply;
CB_Mem_Bool *analog_pump_onCmd;
CB_Mem_Float *analog_pump_setpoint;
CB_Mem_Float *analog_pump_inputFlow;
CB_Mem_Bool *analog_pump_onFB;
CB_Mem_Bool *analog_pump_thermal;
CB_Mem_Float *analog_pump_speedVal;
CB_Mem_Float *analog_pump_outputFlow;
CB_Mem_Float *analog_pump_increment;
CB_Mem_Bool *analog_pump_onState;
CB_Mem_Float *analog_pump_unit;
CB_Mem_Float *analog_pump_speedSetpoint;
CB_Mem_Float *analog_pump_nominalTime;
CB_Mem_Bool *analog_pump_thermalLogic;
CB_Mem_Bool *analog_pump_onFbLogic;
CB_Mem_Float *analog_pump_nominalSpeed;
CB_Mem_Float *analog_pump_flowMax;
CB_Mem_Bool *analog_pump_flowPerHour;
CB_Mem_Bool *analog_pump_flowPerMinute;
CB_Mem_Bool *analog_pump_d_locking;
CB_Mem_Float *analog_pump_v_d_locking;
CB_Mem_Bool *analog_pump_d_flow;
CB_Mem_Float *analog_pump_v_d_flow;
CB_Mem_Bool *analog_pump_d_electrical;
CB_Mem_Bool *analog_pump_v_d_electrical;
CB_Mem_Bool *analog_pump_d_onFB;
CB_Mem_Bool *analog_pump_v_d_onFB;

/**************************** Variables *****************************/

#define voltage380 (analog_pump_voltage380->CB_current_value)
#define cmdSupply (analog_pump_cmdSupply->CB_current_value)
#define sensorSupply (analog_pump_sensorSupply->CB_current_value)
#define onCmd (analog_pump_onCmd->CB_current_value)
#define setpoint (analog_pump_setpoint->CB_current_value)
#define inputFlow (analog_pump_inputFlow->CB_current_value)
#define onFB (analog_pump_onFB->CB_current_value)
#define thermal (analog_pump_thermal->CB_current_value)
#define speedVal (analog_pump_speedVal->CB_current_value)
#define outputFlow (analog_pump_outputFlow->CB_current_value)
#define increment (analog_pump_increment->CB_current_value)
#define onState (analog_pump_onState->CB_current_value)
#define unit (analog_pump_unit->CB_current_value)
#define speedSetpoint (analog_pump_speedSetpoint->CB_current_value)
#define nominalTime (analog_pump_nominalTime->CB_current_value)
#define thermalLogic (analog_pump_thermalLogic->CB_current_value)
#define onFbLogic (analog_pump_onFbLogic->CB_current_value)
#define nominalSpeed (analog_pump_nominalSpeed->CB_current_value)
#define flowMax (analog_pump_flowMax->CB_current_value)
#define flowPerHour (analog_pump_flowPerHour->CB_current_value)
#define flowPerMinute (analog_pump_flowPerMinute->CB_current_value)
#define d_locking (analog_pump_d_locking->CB_current_value)
#define v_d_locking (analog_pump_v_d_locking->CB_current_value)
#define d_flow (analog_pump_d_flow->CB_current_value)
#define v_d_flow (analog_pump_v_d_flow->CB_current_value)
#define d_electrical (analog_pump_d_electrical->CB_current_value)
#define v_d_electrical (analog_pump_v_d_electrical->CB_current_value)
#define d_onFB (analog_pump_d_onFB->CB_current_value)
#define v_d_onFB (analog_pump_v_d_onFB->CB_current_value)


/************************ Components offsets ************************/


/********************* Initialization function **********************/

int _equipment_library__analog_pump_init(void)
{
	(analog_pump_voltage380)->CB_current_value = 1; /*voltage380*/
	(analog_pump_cmdSupply)->CB_current_value = 1; /*cmdSupply*/
	(analog_pump_sensorSupply)->CB_current_value = 1; /*sensorSupply*/
	(self.Float+3)->CB_current_value = 2.0; /*nominalTime*/
	(self.Bool+2)->CB_current_value = 1; /*onFbLogic*/
	(self.Float+4)->CB_current_value = 3600.0; /*nominalSpeed*/
	(self.Float+5)->CB_current_value = 10000.0; /*flowMax*/
	(self.Bool+3)->CB_current_value = 1; /*flowPerHour*/
	self.Bool+=11;
	self.Float+=8;


	return 0;
}


/************************ Behavior function *************************/

int equipment_library__analog_pump(void)
{
	if (exec_term_node(self_num)) {

		/*********************** Internals variables ************************/

		analog_pump_increment = self.Float+0;
		analog_pump_onState = self.Bool+0;
		analog_pump_unit = self.Float+1;
		analog_pump_speedSetpoint = self.Float+2;
		analog_pump_nominalTime = self.Float+3;
		analog_pump_thermalLogic = self.Bool+1;
		analog_pump_onFbLogic = self.Bool+2;
		analog_pump_nominalSpeed = self.Float+4;
		analog_pump_flowMax = self.Float+5;
		analog_pump_flowPerHour = self.Bool+3;
		analog_pump_flowPerMinute = self.Bool+4;
		analog_pump_d_locking = self.Bool+5;
		analog_pump_v_d_locking = self.Float+6;
		analog_pump_d_flow = self.Bool+6;
		analog_pump_v_d_flow = self.Float+7;
		analog_pump_d_electrical = self.Bool+7;
		analog_pump_v_d_electrical = self.Bool+8;
		analog_pump_d_onFB = self.Bool+9;
		analog_pump_v_d_onFB = self.Bool+10;

		/********************* Pre-conditions execution *********************/

		/*no Pre-conditions*/

		/************************ Behavior execution ************************/

		{
			/* -----------------------------------------------------------------	*/
			/* Type : Model of Operative Behaviour					*/
			/* Catégorie :   batch							*/
			/* Author : Dassault Systemes						*/
			/* Update date : June 2018							*/
			/* -----------------------------------------------------------------	*/
			/* This module simulates the behaviour of an analog pump and its 	*/
			/* electrical control logic.						*/
			/* ----------------------------------------------------------------	*/


			/* ----------------------------------------------------------------	*/
			/* Management of orders from Control Part					*/
			/*	if there is no power supply for command 				*/
			/*     the contactor doesn't understand the control order		*/
			/* The setpoint can go from 0% up to +100%
			/* ----------------------------------------------------------------	*/
			if (d_electrical) 
				onState=v_d_electrical;
			else
				onState = onCmd & cmdSupply & voltage380;

			/* ----------------------------------------------------------------	*/
			/* Management of  the rotation speed					*/
			/* ----------------------------------------------------------------	*/
			if (d_locking)  speedVal = v_d_locking;
			else {
				/* Compute the increment of speed according to the simulation clock	*/
				if (nominalTime > 0.0)
					increment = (nominalSpeed / nominalTime) * (CLOCK_PERIOD/1000000.0);

				/* Compute the rotation speed setpoint	*/
				if (onState && (setpoint >=0.0) &&  (setpoint <= 100.0)) speedSetpoint = (setpoint / 100.0) * nominalSpeed;
				if (! onState) speedSetpoint = 0.0;
				
				/* Compute the speed ramp	*/
				if ( speedVal < speedSetpoint ) {
					speedVal += increment;
					if (speedVal > speedSetpoint)  speedVal = speedSetpoint;
				}
				if ( speedVal > speedSetpoint ) {
					speedVal -= increment;
					if (speedVal < speedSetpoint)  speedVal = speedSetpoint;
				}
			}

			/* ----------------------------------------------------------------	*/
			/* Management of the thermal feedback					*/
			/* ----------------------------------------------------------------	*/
			if (d_electrical) thermal = v_d_electrical;
			else
				thermal = ! thermalLogic && sensorSupply;

			/* ----------------------------------------------------------------	*/
			/* Management of feedback to Control Part					*/
			/*     if a fault is required, the contactor feed-back 		*/
			/*		 takes a preset state.					*/
			/*	if there is no power supply for sensors 				*/
			/*         contactor feed-back returns nothing				*/
			/*     else contactor feed-back returns the contactor state combined	*/
			/*	    with wiring logic.						*/
			/* ----------------------------------------------------------------	*/
			if (d_onFB) onFB = v_d_onFB;
			else
				onFB = (onCmd ^ ! onFbLogic) && sensorSupply;

			/* ----------------------------------------------------------------	*/
			/* Management of the flow at the outlet of the pump			*/
			/* The flow is computed according to the rotation speed of the pump	*/
			/* and the flow at the inlet of the pump.					*/
			/* This theorical flow rate is upper bounded with the maximal 	*/
			/* manufacturer's flow.							*/
			/* ----------------------------------------------------------------	*/
			if (d_flow)
				outputFlow = v_d_flow;
			else {
				if (nominalSpeed  > 0.0) {	
					if (flowPerHour) unit = 3600.0;
					else if (flowPerMinute) unit = 60.0;
						else unit = 1.0;
						
					outputFlow = ((flowMax * CLOCK_PERIOD / 1000000)  / unit) * (speedSetpoint / nominalSpeed  );
					if (outputFlow > inputFlow) outputFlow = inputFlow;		
				}
				else outputFlow =0.0;
			}

		}

		CB_post_bool(analog_pump_onFB);
		CB_post_bool(analog_pump_thermal);
		CB_post_float(analog_pump_speedVal);
		CB_post_float(analog_pump_outputFlow);

		/******************** Post-conditions execution *********************/

		/*no Post-conditions*/
	}

	/*************************** self update ****************************/

	self.Bool += 11;
	self.Float += 8;

	return 0;
}

