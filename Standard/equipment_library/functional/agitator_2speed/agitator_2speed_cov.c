/*2023-10-03T11:13:03-01:00*/

/********************************************************************
 * agitator_2speed_cov.c
 * 
 * generated by: plcgen 3.1.391
 ********************************************************************/

#include "cb_comp.h"
#include "cb_covs.h"
#include "iec_1131.h"

#include "agitator_2speed.h"



/**************************** Variables *****************************/

CB_Index agitator_2speed__num = 0;
CB_Mem_Bool *agitator_2speed_voltage380;
CB_Mem_Bool *agitator_2speed_cmdSupply;
CB_Mem_Bool *agitator_2speed_sensorSupply;
CB_Mem_Bool *agitator_2speed_highSpeed;
CB_Mem_Bool *agitator_2speed_lowSpeed;
CB_Mem_Bool *agitator_2speed_highSpeedFB;
CB_Mem_Bool *agitator_2speed_lowSpeedFB;
CB_Mem_Bool *agitator_2speed_thermal;
CB_Mem_Float *agitator_2speed_speedVal;
CB_Mem_Float *agitator_2speed_speedPercent;
CB_Mem_Float *agitator_2speed_increment;
CB_Mem_Bool *agitator_2speed_memoHighSpeed;
CB_Mem_Bool *agitator_2speed_memoLowSpeed;
CB_Mem_Bool *agitator_2speed_highSpeedState;
CB_Mem_Bool *agitator_2speed_lowSpeedState;
CB_Mem_Float *agitator_2speed_speedSetpoint;
CB_Mem_Int *agitator_2speed_cnt;
CB_Mem_Bool *agitator_2speed_flipflop;
CB_Mem_Float *agitator_2speed_nominalTime;
CB_Mem_Bool *agitator_2speed_thermalLogic;
CB_Mem_Bool *agitator_2speed_highSpeedFbLogic;
CB_Mem_Bool *agitator_2speed_lowSpeedFbLogic;
CB_Mem_Float *agitator_2speed_nominalLowSpeed;
CB_Mem_Float *agitator_2speed_nominalHighSpeed;
CB_Mem_Bool *agitator_2speed_d_locking;
CB_Mem_Float *agitator_2speed_v_d_locking;
CB_Mem_Bool *agitator_2speed_d_electrical;
CB_Mem_Bool *agitator_2speed_v_d_electrical;
CB_Mem_Bool *agitator_2speed_d_highSpeedFB;
CB_Mem_Bool *agitator_2speed_v_d_highSpeedFB;
CB_Mem_Bool *agitator_2speed_d_lowSpeedFB;
CB_Mem_Bool *agitator_2speed_v_d_lowSpeedFB;

/**************************** Variables *****************************/

#define voltage380 (agitator_2speed_voltage380->CB_current_value)
#define cmdSupply (agitator_2speed_cmdSupply->CB_current_value)
#define sensorSupply (agitator_2speed_sensorSupply->CB_current_value)
#define highSpeed (agitator_2speed_highSpeed->CB_current_value)
#define lowSpeed (agitator_2speed_lowSpeed->CB_current_value)
#define highSpeedFB (agitator_2speed_highSpeedFB->CB_current_value)
#define lowSpeedFB (agitator_2speed_lowSpeedFB->CB_current_value)
#define thermal (agitator_2speed_thermal->CB_current_value)
#define speedVal (agitator_2speed_speedVal->CB_current_value)
#define speedPercent (agitator_2speed_speedPercent->CB_current_value)
#define increment (agitator_2speed_increment->CB_current_value)
#define memoHighSpeed (agitator_2speed_memoHighSpeed->CB_current_value)
#define memoLowSpeed (agitator_2speed_memoLowSpeed->CB_current_value)
#define highSpeedState (agitator_2speed_highSpeedState->CB_current_value)
#define lowSpeedState (agitator_2speed_lowSpeedState->CB_current_value)
#define speedSetpoint (agitator_2speed_speedSetpoint->CB_current_value)
#define cnt (agitator_2speed_cnt->CB_current_value)
#define flipflop (agitator_2speed_flipflop->CB_current_value)
#define nominalTime (agitator_2speed_nominalTime->CB_current_value)
#define thermalLogic (agitator_2speed_thermalLogic->CB_current_value)
#define highSpeedFbLogic (agitator_2speed_highSpeedFbLogic->CB_current_value)
#define lowSpeedFbLogic (agitator_2speed_lowSpeedFbLogic->CB_current_value)
#define nominalLowSpeed (agitator_2speed_nominalLowSpeed->CB_current_value)
#define nominalHighSpeed (agitator_2speed_nominalHighSpeed->CB_current_value)
#define d_locking (agitator_2speed_d_locking->CB_current_value)
#define v_d_locking (agitator_2speed_v_d_locking->CB_current_value)
#define d_electrical (agitator_2speed_d_electrical->CB_current_value)
#define v_d_electrical (agitator_2speed_v_d_electrical->CB_current_value)
#define d_highSpeedFB (agitator_2speed_d_highSpeedFB->CB_current_value)
#define v_d_highSpeedFB (agitator_2speed_v_d_highSpeedFB->CB_current_value)
#define d_lowSpeedFB (agitator_2speed_d_lowSpeedFB->CB_current_value)
#define v_d_lowSpeedFB (agitator_2speed_v_d_lowSpeedFB->CB_current_value)


/************************ Components offsets ************************/


/********************* Initialization function **********************/

int _equipment_library__agitator_2speed_init(void)
{
	(agitator_2speed_voltage380)->CB_current_value = 1; /*voltage380*/
	(agitator_2speed_cmdSupply)->CB_current_value = 1; /*cmdSupply*/
	(agitator_2speed_sensorSupply)->CB_current_value = 1; /*sensorSupply*/
	(self.Float+2)->CB_current_value = 0.5; /*nominalTime*/
	(self.Bool+6)->CB_current_value = 1; /*highSpeedFbLogic*/
	(self.Bool+7)->CB_current_value = 1; /*lowSpeedFbLogic*/
	(self.Float+3)->CB_current_value = 2500.0; /*nominalLowSpeed*/
	(self.Float+4)->CB_current_value = 5000.0; /*nominalHighSpeed*/
	self.Bool+=15;
	self.Int+=1;
	self.Float+=6;


	return 0;
}


/************************ Behavior function *************************/

int equipment_library__agitator_2speed(void)
{
	static char *__comp_name = "equipment_library.agitator_2speed";
	if (exec_term_node(self_num)) {

		/*********************** Internals variables ************************/

		agitator_2speed_increment = self.Float+0;
		agitator_2speed_memoHighSpeed = self.Bool+0;
		agitator_2speed_memoLowSpeed = self.Bool+1;
		agitator_2speed_highSpeedState = self.Bool+2;
		agitator_2speed_lowSpeedState = self.Bool+3;
		agitator_2speed_speedSetpoint = self.Float+1;
		agitator_2speed_cnt = self.Int+0;
		agitator_2speed_flipflop = self.Bool+4;
		agitator_2speed_nominalTime = self.Float+2;
		agitator_2speed_thermalLogic = self.Bool+5;
		agitator_2speed_highSpeedFbLogic = self.Bool+6;
		agitator_2speed_lowSpeedFbLogic = self.Bool+7;
		agitator_2speed_nominalLowSpeed = self.Float+3;
		agitator_2speed_nominalHighSpeed = self.Float+4;
		agitator_2speed_d_locking = self.Bool+8;
		agitator_2speed_v_d_locking = self.Float+5;
		agitator_2speed_d_electrical = self.Bool+9;
		agitator_2speed_v_d_electrical = self.Bool+10;
		agitator_2speed_d_highSpeedFB = self.Bool+11;
		agitator_2speed_v_d_highSpeedFB = self.Bool+12;
		agitator_2speed_d_lowSpeedFB = self.Bool+13;
		agitator_2speed_v_d_lowSpeedFB = self.Bool+14;

		/********************* Pre-conditions execution *********************/

		/*no Pre-conditions*/

		/************************ Behavior execution ************************/

		{
			/* -----------------------------------------------------------------	*/
			/* Type : Model of Operative Behaviour					*/
			/* Catégorie :   batch							*/
			/* Author : Dassault Systemes						*/
			/* Update date : June 2018							*/
			/* -----------------------------------------------------------------	*/
			/* This module simulates the behaviour of a 2-speed motorized		*/
			/* agitator and its electrical control logic.				*/
			/* ----------------------------------------------------------------	*/


			/* ----------------------------------------------------------------	*/
			/* Management of orders from Control Part					*/
			/*	if there is no power supply for command 				*/
			/*     the contactors don't understand the control order		*/
			/*	contactors are linked mechanicaly					*/
			/* ----------------------------------------------------------------	*/
			if (!memoHighSpeed && lowSpeed && cmdSupply) memoLowSpeed = 1;
			if (!lowSpeed || !cmdSupply || d_electrical) memoLowSpeed = 0;
			if (highSpeed && !memoLowSpeed && cmdSupply) memoHighSpeed = 1;
			if (!highSpeed || !cmdSupply || d_electrical) memoHighSpeed = 0;

			/* ----------------------------------------------------------------	*/
			/* Calculation of the on-state of contactors				*/
			/* ----------------------------------------------------------------	*/
			highSpeedState=memoHighSpeed & voltage380;
			lowSpeedState=memoLowSpeed & voltage380;

			/* ----------------------------------------------------------------	*/
			/* Management of the speed ramp						*/
			/* ----------------------------------------------------------------	*/
			if (d_locking)  speedVal = v_d_locking;
			else {
				/* Compute the speed setpoint according to contators' state	*/
				speedSetpoint = 0.0;
				if (highSpeedState) {
					speedSetpoint = nominalHighSpeed;
					if (nominalTime > 0.0)
						increment = (nominalHighSpeed / nominalTime) * (CLOCK_PERIOD/1000000.0);
				}
				else if (lowSpeedState) {
					speedSetpoint = nominalLowSpeed;
					if (nominalTime > 0.0)
						increment = (nominalLowSpeed / nominalTime) * (CLOCK_PERIOD/1000000.0);
				}

				/* Compute the speed ramp	*/
				if ( speedVal < speedSetpoint ) {
					speedVal += increment;
					if ( speedVal > speedSetpoint ) speedVal = speedSetpoint;
				}
				if ( speedVal > speedSetpoint ) {
					speedVal -= increment;
					if (speedVal < speedSetpoint) speedVal = speedSetpoint;
				}
			}

			/* ----------------------------------------------------------------	*/
			/* Compute the percentage between the current and nominal speed	*/
			/* ----------------------------------------------------------------	*/
			if (nominalHighSpeed != 0.0)
				speedPercent = ((speedVal/nominalHighSpeed)*100);

			/* ----------------------------------------------------------------	*/
			/* Management of the thermal feedback					*/
			/* ----------------------------------------------------------------	*/
			if (d_electrical) thermal=v_d_electrical;
			else
				thermal= ! thermalLogic && sensorSupply;

			/* ----------------------------------------------------------------	*/
			/* Management of feedback to Control Part					*/
			/*     if a fault is required, the contactor feed-back 		*/
			/*		 takes a preset state.					*/
			/*	if there is no power supply for sensors 				*/
			/*         contactor feed-back returns nothing				*/
			/*     else contactor feed-back returns the contactor state combined	*/
			/*	    with wiring logic.						*/
			/* ----------------------------------------------------------------	*/
			if (d_highSpeedFB) highSpeedFB = v_d_highSpeedFB;
			else 	
				highSpeedFB = (memoHighSpeed ^ ! highSpeedFbLogic) && sensorSupply;

			if (d_lowSpeedFB) lowSpeedFB = v_d_lowSpeedFB;
			else	
				lowSpeedFB = (memoLowSpeed ^ ! lowSpeedFbLogic) && sensorSupply;

			/* ----------------------------------------------------------------	*/
			/* Animation of blades : only for MAN					*/
			/* ----------------------------------------------------------------	*/
			if (speedPercent != 0.0) {
				if (! flipflop) cnt ++;
				else cnt --;
				if (cnt >4) {
					cnt = 4;
					flipflop = 1;
				}
				if (cnt <= 0) flipflop = 0;
			}


		}

		CB_post_bool(agitator_2speed_highSpeedFB);
		CB_post_bool(agitator_2speed_lowSpeedFB);
		CB_post_bool(agitator_2speed_thermal);
		CB_post_float(agitator_2speed_speedVal);
		CB_post_float(agitator_2speed_speedPercent);

		/******************** Post-conditions execution *********************/

		/*no Post-conditions*/
	}

	/*************************** self update ****************************/

	self.Bool += 15;
	self.Int += 1;
	self.Float += 6;

	return 0;
}

