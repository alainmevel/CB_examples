/*2023-10-03T11:12:51-01:00*/

/********************************************************************
 * range_sensor.c
 * 
 * generated by: plcgen 3.1.391
 ********************************************************************/

#include "cb_comp.h"
#include "iec_1131.h"

#include "range_sensor.h"



/**************************** Variables *****************************/

CB_Index range_sensor__num = 0;
CB_Mem_Float *range_sensor_level;
CB_Mem_Bool *range_sensor_sensorSupply;
CB_Mem_Bool *range_sensor_rangeSensor;
CB_Mem_Float *range_sensor_threshold;
CB_Mem_Float *range_sensor_range;
CB_Mem_Bool *range_sensor_wiringLogic;
CB_Mem_Bool *range_sensor_d_sensor;
CB_Mem_Bool *range_sensor_v_d_sensor;

/**************************** Variables *****************************/

#define level (range_sensor_level->CB_current_value)
#define sensorSupply (range_sensor_sensorSupply->CB_current_value)
#define rangeSensor (range_sensor_rangeSensor->CB_current_value)
#define threshold (range_sensor_threshold->CB_current_value)
#define range (range_sensor_range->CB_current_value)
#define wiringLogic (range_sensor_wiringLogic->CB_current_value)
#define d_sensor (range_sensor_d_sensor->CB_current_value)
#define v_d_sensor (range_sensor_v_d_sensor->CB_current_value)


/************************ Components offsets ************************/


/********************* Initialization function **********************/

int _equipment_library__range_sensor_init(void)
{
	(range_sensor_sensorSupply)->CB_current_value = 1; /*sensorSupply*/
	(self.Float+0)->CB_current_value = 500.0; /*threshold*/
	(self.Float+1)->CB_current_value = 10.0; /*range*/
	(self.Bool+0)->CB_current_value = 1; /*wiringLogic*/
	self.Bool+=3;
	self.Float+=2;


	return 0;
}


/************************ Behavior function *************************/

int equipment_library__range_sensor(void)
{
	if (exec_term_node(self_num)) {

		/*********************** Internals variables ************************/

		range_sensor_threshold = self.Float+0;
		range_sensor_range = self.Float+1;
		range_sensor_wiringLogic = self.Bool+0;
		range_sensor_d_sensor = self.Bool+1;
		range_sensor_v_d_sensor = self.Bool+2;

		/********************* Pre-conditions execution *********************/

		/*no Pre-conditions*/

		/************************ Behavior execution ************************/

		{
			/* ----------------------------------------------------------------	*/
			/* Type : Model of Operative Behaviour					*/
			/* Category :  Sensors							*/
			/* Author : Dassault Systemes						*/
			/* Update date : June 2018							*/
			/* ----------------------------------------------------------------	*/
			/* This module simulates the behaviour 					*/
			/* of an over threshold sensor with a detection area.			*/
			/* ----------------------------------------------------------------	*/
			/* In the case of a faulty element, it takes the fault value else 	*/
			/* it takes the state of the sensor in function of its wiring logic.	*/ 
			/* ----------------------------------------------------------------	*/

			if (d_sensor) rangeSensor = v_d_sensor;
			else
				if ( sensorSupply ) rangeSensor = ((level >= threshold) & (level <= (threshold + range))) ^ ! wiringLogic;
				else rangeSensor = 0;

		}

		CB_post_bool(range_sensor_rangeSensor);

		/******************** Post-conditions execution *********************/

		/*no Post-conditions*/
	}

	/*************************** self update ****************************/

	self.Bool += 3;
	self.Float += 2;

	return 0;
}

